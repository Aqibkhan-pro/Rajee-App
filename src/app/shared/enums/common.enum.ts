import { Range } from './../../../graphql/generated';
export enum ToastType {
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'danger',
}

export enum ToastPosition {
  TOP = 'top',
  BOTTOM = 'bottom',
}

export enum Segment {
  REPAIR = 'Repair',
  QC = 'QC',
  CLEAN = 'Clean',
  DEVICES = 'Devices',
  PARTS = 'Parts',

}
